# 📜 Utility Scripts & Database Initialization

This directory contains shell and SQL scripts for database provisioning and seeding.

---

## Files
- `init-db.sql`: Initializes PostgreSQL database extensions (`uuid-ossp`, `pgcrypto`) and the `public` schema (`tenants`, `audit_logs`).
